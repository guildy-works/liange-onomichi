import { css } from "@emotion/react";
import React from "react";

export default () => {
    return (
        <footer css={css({ padding:"20px"})}>
            this is footer
        </footer>
    );
};
